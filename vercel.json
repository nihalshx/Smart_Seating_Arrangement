{
    "version": 2,
    "builds": [
        {
            "src": "wsgi.py",
            "use": "@vercel/python",
            "config": {
                "maxLambdaSize": "50mb",
                "runtime": "python3.9"
            }
        }
    ],
    "routes": [
        {
            "src": "/static/(.*)",
            "dest": "/static/$1"
        },
        {
            "src": "/(.*)",
            "dest": "/wsgi.py"
        }
    ],
    "env": {
        "FLASK_APP": "minimal_app.py",
        "FLASK_ENV": "production",
        "PYTHONUNBUFFERED": "1",
        "FLASK_SECRET_KEY": "temporary-secure-key-replace-in-vercel-dashboard"
    }
}